# Completion for list_secrets function

# This function doesn't take arguments, so we just provide a description
complete -c list_secrets -f -d "List all available secrets without showing their values"
