# Completion for load_secrets function

# This function doesn't take arguments, so we just provide a description
complete -c load_secrets -f -d "Load all secrets from encrypted storage into environment variables"
