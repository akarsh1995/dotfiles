# Completions for pwstore_export function
complete -c pwstore_export -f -d "Export passwords to a GPG-encrypted backup file"
complete -c pwstore_export -a "(__fish_complete_path)" -d "Export file path"
