# Completions for pwstore_list function
complete -c pwstore_list -f -d "List all passwords in the password store"
complete -c pwstore_list -l details -d "Show additional details including update times"
