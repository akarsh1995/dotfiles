# Completion for secret_load function

# This function doesn't take arguments, so we just provide a description
complete -c secret_load -f -d "Load all secrets from encrypted storage into environment variables"
